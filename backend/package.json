{
  "name": "synapse-ledger-backend",
  "version": "0.1.0",
  "description": "Backend server for Synapse Ledger — handles Sentinel Unit data ingestion, validation, and blockchain writes.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "web3": "^4.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
